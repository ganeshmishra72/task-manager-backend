server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/taskmanagementsys
    username: root
    password: Ganesh@2005
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope:
            - email
            - profile
            - openid
          github:
            client-id: ${GITHUB_CLIENT_ID:123} 
            client-secret: ${GITHUB_CLIENT_SECRET:123}
            scope:
            - user.email
            - read.user
logging:
  level:
    org.springframework.security: DEBUG

security:
  jwt:
      secert: ${JWT_SECERET:sfskjkvdslkhhlhvlnfdhogiouenurwvnrwimwryowuvrgrgu09jirwtycntuwrtkcj4ytu9cik0ti3mcbtuyruomnbfafrshfgnmljuyfd}
      issuer: ${JWT_TOKEN:api.rdmishra.com}
      access-ttl-seconds: ${JWT_ACCESS_TTL_SECONDS:600}
      refresh-ttl-seconds: ${JWT_REFRESH_TTL_SECONDS:86400}
      refresh-token-cookie-name: ${JWT_REFRESH-COOKIE_NAME:refreshToken}
      cookie-secure: ${JWT_COOKIE_SECURE:true}
      cookie-http-only: ${JWT_COOKIE_HTTP_ONLY:true}
      cookie-same-site: ${JWT_COOKIE_SAME_SITE:none}
      cookie-domain-name: ""

app:
  auth:
      frontend.success-url : http://localhost:3000
  cors:
    frontend-url : http://localhost:3000

cloudinary:
  cloud-name: dy25vlvd5
  api-key: 342245661539857
  api-secret: 1J4WEdSMv2JIpWsKaUysKnhRd8I 